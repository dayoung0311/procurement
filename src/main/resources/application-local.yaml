server:
  servlet:
    context-path: /procurement

spring:
  application:
    name: procurement

  threads:
    virtual:
      enabled: true

  datasource:
    url: ${DB_ENDPOINT:jdbc:postgresql://localhost:5432/procurement}
    username: ${DB_NAME:procurement}
    password: ${DB_PASSWORD:procurement}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        use_sql_comments: true
        jdbc:
          time_zone: Asia/Seoul

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

  kafka:
    bootstrap-servers: kafka.inwoohub.com:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
    consumer:
      group-id: procurement-purchase
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer

springdoc:
  swagger-ui:
    tags-sorter: alpha
    operations-sorter: alpha
    config-url: /procurement/v3/api-docs/swagger-config
    url: /procurement/v3/api-docs

item:
  base-url: ${ITEM_SERVICE_BASE_URL:http://bbd-item:8080/item}

sales:
  base-url: ${SALES_SERVICE_BASE_URL:http://bbd-sales:8080/sales}

logging:
  level:
    root: info
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: trace

